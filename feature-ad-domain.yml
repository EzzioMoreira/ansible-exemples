---
 # This playbook install windows feature AD on server
 - name: Install AD windows server
   hosts: all
   gather_facts: false
   tasks:
     - name: Install AD
       win_feature:
         name: AD-Domain-Services
         state: present
